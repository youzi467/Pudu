# 谱渡 Pudu · 产品状态与决策矩阵

> 生成：2026-08-09
> 定位：产品向对外口径与后续路线的一页决策文档。技术账本见 `docs/f3-abtest.md`（84.5% 封账清单）、
> `docs/next-steps.md`（工程路线图）。本页只讲「对外怎么说、接下来投哪」。
> 状态：四项决策已拍板（2026-08-09），见下。

---

## 0. 一句话定位

**五线谱图片 → MusicXML（一等输出）+ 简谱（内部投影）** 的 OMR 引擎：
**默认走 Audiveris**（`tools/omr_audiveris.py` 子进程调用 `Audiveris.exe -batch`，自带 JRE，
图像 glyph 检测，支持多页 PDF 逐页拼接）产出 MusicXML，再由 Pudu 内核投影为简谱；
**oemer（+F3/R-geo 几何校正）保留为 `--omr-engine oemer` 回退分支**（2026-08-12 A/B
在 keysig/时值/小节三项全面胜出 +13.1pp 后迁移，见 docs/audiveris-ab-verdict.md）。
**MusicXML 是产品交付物，不是中间格式。**

---

## 1. 能力剖面（AV 权威基线 · 13 共有页 / 3197 音符）

| 指标 | AV（默认引擎） | oemer 回退（历史） | 说明 |
|---|---|---|---|
| `note_pass_rate` | **97.56%** (3119/3197) | 84.5% (2656/3143) | 音符级联立（最严格口径）；AV=13 共有页公平对比，+13.1pp（2026-08-12 A/B 定盘） |
| `field_pass_rate` | **94.43%** | 91.7% | 字段级独立通过率 |
| keysig（调号） | **13/13 全对** | 9/13（欠数一升降） | AV 图像 glyph 检测；用户 canon 投诉 #1 修复 |
| rhythm（时值） | **99.26%**（错误 24） | 88.99%（错误 346） | AV 无 R-geo 塌缩（canon_p1 109→2） |
| 拍号 | 检出 7 页全对 | 零输出（下游注入） | 非 4/4 拍（2/4、3/4、6/4）直接读出 |
| 页级 | **14/15 正常，仅 canon_p2** | 13/15（canon_p2、summer_p5） | summer_p5 被 AV 救回 100% (25/25) |
| 页最低 | prelude_p2 91.8% | bach_p3 10.5% | AV 13 页全部 ≥88.7%，无页级塌缩 |
| MusicXML 结构 | `<beam>/<accidental>/<tie>` 原生产出 | 天然缺失 | 结构缺口在 AV 下补齐（见 §4） |

> AV 基线来源：`build/_av_eval13`（97.56% / 94.43%，2026-08-12 A/B 定盘，公平对比=13 共有页）。
> oemer 84.5% / 91.7% 保留为 `--omr-engine oemer` 回退口径（其前身 3147→3144→3143 音符数为
> forward 溢出修复链的 2026-08-10 定盘，见 docs/f3-abtest.md 账本）。

---

## 2. 对外指标口径（决策更新：AV 主口径）

**主口径：音符级联立 97.56%（AV 权威基线）**；注明字段级 94.43%；**oemer 回退口径（84.5%/91.7%）
只在 `--omr-engine oemer` 回退路径被触发时陈述**。

| 口径 | 数字 | 定位 | 适用场景 |
|---|---|---|---|
| 音符级联立 note_pass | **97.56%**（AV，13 共有页） | **对外主口径** | 能力陈述、对标、技术文档、A/B 复用 |
| 字段级 field_pass | 94.43%（AV） | 辅助口径 | 单维度能力陈述（音高 / 时值 / 调号） |
| oemer 回退 | 84.5% / 91.7% | 回退路径口径，仅在引擎回退时陈述 | `--omr-engine oemer` |
| 分维度 | 时值 99.26%、keysig 13/13 | 可解释 | 面向音乐用户、功能宣传 |

原则：**AV 为主口径，oemer 只在回退时陈述；低阶口径永远备查。**

---

## 3. 精度路线（决策更新：AV 迁移已落地为默认主线）

**2026-08-12 路线变更**：Audiveris A/B 在 keysig/时值/小节三项全面胜出（note_pass +13.1pp），
**迁移已实施**——AV 为默认引擎，oemer 保留为 `--omr-engine oemer` 回退。原 84.5% 定盘态的
失败音符分账（487 错：rhythm 308 / accidental 60 / degree 25 / octave 10 / tie 13）与
D/C 取舍保留为**回退口径**的账本（见 docs/f3-abtest.md）。

| 方向 | 结论 |
|---|---|
| **AV 主线（已立项 / 已实施）** | 97.56% 定盘（13 共有页）；无 R-geo 塌缩、keysig 13/13、拍号图像检测、`<beam>/<accidental>/<tie>` 原生产出；残余错源=AV 13 页 78 音符 + 唯一失败页 canon_p2 |
| **C. 图像级符号检测** | 在 AV 下**大部分已天然满足**（AV 已产 ♯/♭、梁、延音弧线）；仅对 oemer 回退分支仍有意义，**ROI 大幅下降** |
| D. oemer 84.5% 收尾 | 已被 AV 取代，仅作回退分支封账 |
| A. 页级门控（跳过失败页） | ❌ 维持否决（纯 eval 过拟合） |
| B. swan 校准修复 | ❌ 维持暂缓 |

> AV 残余边界（诚实）：canon_p2 `No system found`（图像不可读，两引擎均败）逐页 skip +
> 明确报错；多页 PDF 后继页无拍号符号 → 继承页 1 拍号 / 打标兜底。

---

## 4. MusicXML 一等输出承诺 + 验收口径（决策已定）

**产品链路**：`五线谱图片 → MusicXML（一等输出）→ 简谱（内部投影）`。
MusicXML 是用户可交付的产物，供打谱软件 / DAW / 教育生态消费，简谱由它投影。

**验收口径（扩为双项）**：

| 验收项 | 内容 | 当前状态 |
|---|---|---|
| ① 简谱正确率 | `note_pass` / `field_pass` | **97.56% / 94.43%**（AV 权威基线，13 共有页，2026-08-12 定盘）；oemer 回退 84.5% / 91.7% |
| ② MusicXML 结构有效 | **music21 可读 + 可再渲染**（schema 有效、可被下游消费） | **AV 原生产出 `<beam>/<accidental>/<tie>`**——oemer 时代的结构缺口在 AV 下补齐（用户 canon 投诉 #2 的时值/小节自洽也随 rhythm 99.26% 解决）；单页与多页拼接产物 music21 可读已验证；oemer 回退仍受 voice-bridge 错位限制（bach_p2/bach_p3 可读不可再渲染，见 §7） |

**影响**：AV 一次迁移同时拿到简谱精度（+13.1pp）与 MusicXML 结构保真度
（`<beam>/<accidental>/<tie>`），C 路线的双受益价值在默认引擎下已天然兑现。

---

## 5. 兜底体验与泛化（决策已定）

| 子项 | 决策 |
|---|---|
| **低置信度标记** | ✅ **已落地**（2026-08-10）：对 F3/R-geo 跳过音符写 `<notations><footnote>需校对：{原因}</footnote>`（MusicXML 3.1；Pudu 按名分发忽略 `notations`→eval 零影响已验证，music21 可读可再渲染）。语料 29 音符被标、全为「几何时值未校正」（初始 65 中 36 个在模糊边界 ratio≈.5 上，随 R-geo 置信窗修复剔除——见 f3-abtest.md 账本）。**读短/失败页标记已弃**：196 读短簇几何+beam 双盲，实测 `cls>2×当前` 抓 0 真读短 |
| 失败页降级 | 明确报错（诚实）优先，不做静默半成品 |
| 语料泛化 | 同语料全链路重跑已复现 84.5%（2026-08-10）；扩到新真实 PDF 再验泛化（后续） |

---

## 6. 简谱小节质量修复（决策已定：方案1 → 归因 → 方案4 + 方案2）

**投诉**：简谱输出小节时值不对、每小节节拍数不对。
**归因（2026-08-10，13 页 350 voice-measure 逐对核对）**：坏小节**主导根因 = oemer 小节
边界错位**——音符音高时值全对，只是被插/漏纵线（铁证 bach p1 开头切 2/14/4/12，正确
16/16，逐音核对全对）；**不是缺休止**（79 个欠填小节 GT rest-sum 全为 0）。音符真丢失仅
个别页（canon p1 32.6%、summer p2 17.1%、the-swan 11.5% 等，oemer 检测 dropout，
Python 不可修）。

| 方案 | 内容 | 结论 |
|---|---|---|
| **1. 拍号推断 + 注入 `<time>`** | 从既有 `<time>`/小节填充推断拍号写入 MusicXML | ✅ **已落地**（inject_time_signature，纯 Python，FROZEN 零 diff；11/11 页推断正确） |
| **归因** | 精确判定坏小节根因 | ✅ 见上，为 3/4 取舍提供依据 |
| **3. 补休止** | 欠填小节补 `<rest>` 填满 | ❌ **否决**：假休止盖在错位/丢失音符上，节拍数好看但音符更错 |
| **4. 拍号约束保守重切** | 音符流按拍号 target 贪心重切；门（碎片页 / 切点偏差>tol / 无拍号）外不改文件 | ✅ **已落地**（re_slice_measures）；**GT 验证 beatOK 100→171（+71 小节）**，6 页重切（bach p1、badinerie、prelude p1/p2、summer p1/p4），音符级质量不变 |
| **2. 节拍校验打标** | `\|fill−target\|>tol` 小节给首音符打 `<footnote>需校对：小节节拍不符</footnote>` | ✅ **已落地**（mark_meter_constraint_failures）；门失败页（canon p1、swan-lake、the-swan、bach p2/p3、summer p2/p3）兜底 |

**落地管线**（pudu_server 定点阶段）：`注入 <time> → 保守重切 → 节拍校验打标`，与 FROZEN
管线零 diff。
**边界**：重切只修「错位」，**找不回真丢失音符**（canon p1、the-swan 等仍打标兜底，不谎报）；
弱起小节（badinerie GT m0 pickup）与碎片页不重切。验证仅限既有语料同页（先不跑泛化）。

---

## 7. 诚实边界（对外务必一致）

**AV 默认引擎（97.56% 口径）**：
- **97.56% 是最严格数字**（13 共有页音符级联立；字段级 94.43%）；
- **1/15 页失败**：canon_p2 `No system found`（图像不可读，两引擎均败），逐页 skip + 明确报错；
- **多页 PDF 后继页无拍号符号**：AV 逐页处理，页 2+ 缺 `<time>` → 继承页 1 拍号 / `mark_meter_constraint_failures` 打标兜底；
- **「需校对」标注沿拍号校验**：AV 无 oemer 几何 sidecar 的低置信概念，节拍不符小节打 `<footnote>`「需校对：小节节拍不符」兜底。

**oemer 回退（84.5% 口径，仅 `--omr-engine oemer` 触发）**：
- **84.5% 是最严格数字**：联立惩罚放大，字段级 91.7% 才是单维度真实水平；
- **节奏是弱项**：残余读短/读长 348 已几何+beam 双盲封账，非短期可修；
- **图像级符号未检测**：♯/♭、梁、延音弧线 oemer 不产，当前靠几何/结构兜底；
- **2/15 页失败**：oemer 检测层崩溃（canon_p2、summer_p5），明确报错降级；
- **forward 溢出已修复**（2026-08-10：移除 51 个 corrupt `<forward>`）；**残余 bach_p2/bach_p3 可读但不可再渲染**（voice-bridge 把 v2 音符写到负偏移/错位），列为已知边界。

---

## 8. 维护约定

- 数字更新请同步 `docs/f3-abtest.md`（权威技术账本）与本页 §1/§7；
- C 路线立项后在本页 §3 记录投入与收益；
- MusicXML 验收项 ② 落地后在本页 §4 更新状态。
