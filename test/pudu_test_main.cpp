// ----------------------------------------------------------------------
// 谱渡 Pudu · 单元测试入口
//   收集所有 TEST 注册项并运行，退出码：全绿 0 / 有失败非 0。
// ----------------------------------------------------------------------

#include "pudu_test.hpp"

int main() {
    return ::pudu_test::runAll() == 0 ? 0 : 1;
}
