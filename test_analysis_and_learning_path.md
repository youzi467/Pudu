# 谱渡 Pudu — 测试构成、验证总结与学习路径（基于 117 个 gtest 用例 + 41 个 F3 Python 单测 全绿）

> 生成日期：2026-07-15 ｜ 状态基准：`main = 12849dd`（阶段2 squash + 合并 origin/main），本地 `ctest` 实测 **70/70 全绿**（原 54 + 变调 16）。
> 本文档取代早期 `learning_path.md` 的阶段划分，并以本次单测结果为依据重新梳理学习路径。
> **现状刷新（2026-07-21 后）**：当前 `HEAD == origin/main == 0736c92`，已全部推送、无未推送提交；**117 个 gtest 用例（经 1 个 ctest 入口 `PuduTests` 运行）全绿 + 41 个 F3 Python 单测全绿**；阶段1 OMR 与阶段3 反向均已落地（下文状态已同步）。

---

## 一、70 个测试用例构成说明

### 1.1 分类依据

用例按 **「依赖层级 + 功能模块」** 二维划分：

- **依赖层级（决定优先级）**：从最底层、零依赖的纯函数，到依赖前者的转换管线，再到只消费模型的渲染器，最后是新接入的变调功能。层级越低，被复用面越广，一旦回归影响越大 → 优先级越高。
- **功能模块（决定覆盖面）**：调性数学、音高映射、时值映射、转换管线、三渲染器、变调重算。

### 1.2 数量分布表

| 文件 | 模块 | 用例数 | 层级 | 优先级 |
|------|------|:---:|------|:---:|
| `test_tonic.cpp` | 调性 / 主音参考系（圆号圈 ↔ 音级） | 13 | L0 纯函数 | P0 |
| `test_pitch_mapping.cpp` | 音高 MIDI → 简谱数字（含八度点/升降号） | 9 | L0 纯函数 | P0 |
| `test_duration.cpp` | 时值 → 增/减时线、附点 | 9 | L0 纯函数 | P0 |
| `test_staff_to_jianpu.cpp` | 五线→简谱 端到端 + L1/L2/L3 渲染 | 23 | L1 管线 + L2 渲染 | P1 / P2 |
| `test_transpose.cpp` | 变调重算（移调 / 改调号 / 解析） | 16 | L1.5 新功能 | P1 |
| **合计** | | **70**（阶段2 时代口径；当前总计 **117 个 gtest 用例 + 41 个 F3 Python 单测**） | | |

`test_staff_to_jianpu.cpp` 内部再细分：
- `staffToJianpu_*` 转换核心 **15**（P1）
- `jianpuToL1_*` 文本渲染 **2**（P2）
- `jianpuToL2_*` HTML 渲染 **4**（P2）
- `jianpuToJson_*` 结构化渲染 **2**（P2）

### 1.3 覆盖的功能模块与场景范围

**L0 纯函数（31 例）— 系统数学地基**
- `fifthsToTonicPc`：覆盖全部常用调号（C=0/G=7/D=2/A=9/E=4/B=11/F#=6/F=-5 环绕为正/ Bb=10），含越界环绕 `large_fifths_still_in_range`。
- `fifthsToTonicName`：规范调名输出、越界 clamp、小调用关系大调首调法。
- `midiToJianpu` / `midiToDegree`：C4/G4/C5/C3 基础数字与八度；D 大调下 G#4=升4、C4=降7 的拼写；`midiToDegree` 与 `midiToJianpu` 数字一致性；非法 step / 休止符安全不抛。
- `typeToDuration`：全音符到 64 分音符的增时线/减时线数；未知 type、空 type 稳健回退为四分音符。

**L1 转换管线（15 例）— 五线→简谱核心**
附点保留、全/八分休止、三和弦度数存储、多声部分裂成多行、装饰音标记、跨小节连音线、节奏由 `quarterLength` 覆盖错误 `<type>`、附点节奏、连音组(tuplet)回退到 `<type>`、连音组标注、非连音时 tuplet 字段为 0、连音组基准节奏匹配、表头透传、空谱返回空 doc。

**L2 渲染器（8 例）— 三种输出**
- L1 文本：表头+音符渲染、空 doc 安全。
- L2 HTML：自包含 HTML、连续八分音符连尾(beam)、高/低八度点、空 doc 安全。
- L3 JSON：单音基础结构、空 doc 安全。

**L1.5 变调重算（16 例）— 新接入功能**
- 解析：大调/降号调/小调调名、后缀与容错（`m`/`minor`/`小调`/`#`/`b`/`♯`/`♭`）、非法调名抛错、返回 mode、`semitonesToFifths` 推导标签调。
- 核心：移到 D 大调保持数字、字面半音移调与按调名一致、移调同步 Score 音高与标签、空谱抛错、改调号(rekey)保音高重算数字、休止不变、和弦全音平移、升号调用 ♯、降号调用 ♭。

**场景类型分布（按路径性质）**
- 正常路径（happy path）：约 **55** 例。
- 边界 / 异常 / 空输入（robustness）：约 **15** 例 —— 圆号圈越界环绕(clamp)、type 未知/空、非法 step、休止符、空谱（转换层与三渲染器各 1）、非法调名、空 Score。

### 1.4 用例间优先级关系

```
        P0 地基（31）               P1 管线/新功能（31）          P2 渲染（8）
  ┌─────────────────────┐    ┌──────────────────────────┐   ┌──────────────┐
  │ tonic(13)           │    │ staffToJianpu(15)         │   │ L1(2) L2(4)  │
  │ pitch_mapping(9) ───┼──► │ transpose(16) ────────────┼──►│ L3(2)        │
  │ duration(9)         │    │   （复用上方纯函数）        │   │ （仅消费 L0）│
  └─────────────────────┘    └──────────────────────────┘   └──────────────┘
        被所有人依赖              依赖 L0，变调额外回写 Score       叶子，无下游依赖
```

- **P0 > P1 > P2**：L0 纯函数是唯一真理源，被转换管线、变调、渲染共同依赖。若 L0 回归，下游全部失真，故权重最高。
- L1 与 L1.5 同级（P1）：变调是新功能但**完全复用 L0 纯函数**，未引入新数学，风险隔离在"主音参考系替换"这一处。
- P2 渲染是叶子节点：只消费 L0，不被任何模块依赖，回归影响面最小，故优先级最低但仍是用户可见质量门。
- 边界/异常用例分散在各层，构成"安全网"——确保空输入、非法输入、越界不崩溃。

---

## 二、测试验证总结

### 2.1 本次验证的功能点与质量指标

**功能点（白盒，单测层面）**
1. 调性数学：圆号圈 ↔ 音级双向映射正确，含环绕、越界、小调相对大调法。
2. 音高映射：MIDI → 简谱数字/八度点/升降号拼写正确。
3. 节奏映射：`<type>` → 增/减时线/附点正确，异常 type 稳健。
4. 转换管线 `staffToJianpu`：附点、休止、和弦、多声部、装饰音、连音线、节奏 quarterLength 覆盖、连音组标注与回退全部正确。
5. 三渲染器：L1 文本 / L2 HTML（自包含、连尾、八度点）/ L3 JSON 各自正确且对空谱安全。
6. 变调重算：调名解析（大小调/升降号/容错）、移调保数字改听感、字面半音与按调名一致、移调同步 Score、rekey 保音高重算数字、休止/和弦处理、升/降号调拼写偏好——全部正确。

**质量指标**
- **通过率 100%**（117 个 gtest 用例全绿 + 41 个 F3 Python 单测全绿）。
- **编译质量**：MSVC `/utf-8` + 显式 include（已修 `transpose.cpp` 缺 `<stdexcept>`/`<cstdlib>`），跨编译器一致（MSVC 构建下 117 个 gtest 用例全绿 + 41 个 F3 Python 单测全绿）。
- **可回归性**：CTest 已注册（`enable_testing()` + `add_test`），可在构建目录一键 `ctest --test-dir build --output-on-failure`。
- **外部 corroboration**（历史）：music21 跨语言 ground-truth 校验此前已达 音符/字段均 100%、计入差异=0——单测（白盒）与之（黑盒）互相印证。

### 2.2 对后续开发流程的意义

- **集成（阶段3 反向转换）**：本次验证锁定了 `JianpuDoc` / `JianpuNote` 的契约稳定性与 `staffToJianpu` 的可逆前提。阶段3 `jianpuToStaff` 可直接以 L0 为输入反向重建 `Score`，且变调重算已保证「L0 的 `fifths/tonicLabel` 与实际 pitch 自洽」→ G3 round-trip 音高守恒测试有了可信地基。
- **部署**：CLI 已具备生产可用形态（`--to-jianpu` / `--to-jianpu-l2` / `--to-jianpu-json` / `--key` / `--rekey` / `--transpose`）。当前**无 GUI、无安装包、无 CI**，部署仍限于「源码 + 构建」。
- **上线（MVP v1 收尾）**：阶段2 功能闭环已达成，但上线前必须完成：
  1. **M0 推送** 已完成（`origin/main == 0736c92`，阶段2 + 变调 + 阶段3 + OMR + F3 已全部推送，无未推送提交）。
  2. **research_report 状态回填**（文档收尾，须在所有代码完成后做）。
  3. 剩余边界项（见 2.3）可视上线标准决定纳入与否。

### 2.3 已知风险与未覆盖区域

**已知限制（已在设计内接受）**
- **极端连音比 46 处**（7:8 / 7:4 / 9:4）：双边均无法映射标准时值，已单列不计校验分母。属音乐学边界，非缺陷。
- **小调 6=X、和弦逐音八度点、L2 美化**：阶段2 边界项，尚未补；不阻断阶段3，但影响记谱完整度。
- **等音异名拼写**：变调按目标调号偏好选 ♯/♭，未做上下文最优拼写（如增和弦用 ♯、减和弦用 ♭）。

**未覆盖区域（测试盲区）**
- **无端到端 CLI 集成测试**：尚未有「喂入 `data/*.musicxml` → 比对输出」的测试，单测仅白盒。
- **无覆盖率统计 / 无 CI / 无 property-based(fuzz) 测试**。
- **阶段1 OMR 黑盒、阶段4 AI、阶段5 GUI**：完全未覆盖（尚未实现）。
- **变调 × 多声部 × 连音组组合**：单测覆盖了各自维度，但交叉组合（如变调后连音组 round-trip）待阶段3 G3 一并验证。
- **OpenCV 基础（S4–S5）**：按架构决策降级为选做，未接入。

---

## 三、学习路径规划（基于当前测试结果重梳）

> 原则：以「可验证的契约」为学习锚点。每一阶段都以"让对应单测由红转绿 / 保持全绿"为里程碑，避免空学。

### 阶段 0 — 环境与解析地基 ✅ 已完成
- **目标**：跑通 C++20/CMake/vcpkg/pugixml 工具链，建立 MusicXML → `Score` 解析层。
- **任务**：配置 Ninja+vcpkg；实现 `musicxml_parser`；80/80 corpus 解析 PASS；修复 4 项历史缺陷。
- **产出**：`Score`/`Note`/`Credit` 模型、`musicxml_parser`。
- **衔接**：`Score` 是阶段2/阶段3 的共同输入与输出，是后续所有模块的契约边界。

### 阶段 1 — OMR 黑盒集成 ✅ 已完成（M2：oemer 黑盒集成 + 评测 harness + Plan A + H2）
- **目标**：把 PDF/JPG → MusicXML 的外部识别能力接入流水线（黑盒，不自己写识别）。
- **任务**：选型/封装 OMR 工具；定义其输出到 `Score` 的适配层；处理识别误差与人工校正回路。
- **产出**：`omr_adapter` + 输入预处理。
- **衔接**：产出的 `Score` 直接喂给阶段2 转换；与阶段2/阶段3 **无代码耦合**，可随时插入。
- **为何后置**：纯 C++、风险最低的"双脑"反向转换（阶段3）先做完，能让工具在拿到任意 `Score` 前就具备完整读写能力，OMR 只需负责"读"一端。

### 阶段 2 — 五线→简谱 ✅ 已完成（含变调重算）
- **目标**：`staffToJianpu` + L1/L2/L3 渲染 + 变调重算，达成 MVP v1。
- **任务**：实现 `JianpuDoc` 模型、转换器、三渲染器、变调模块；补齐单测至 70/70。
- **产出**：CLI（`--to-jianpu*` / `--key` / `--rekey` / `--transpose`）、70 单测、README、阶段3 计划。
- **衔接**：**L0 契约已锁定** → 是阶段3 的反向输入；变调保证 round-trip 音高自洽 → 是 G3 前置。

### 阶段 3 — 简谱→五线谱（反向转换）✅ 已完成（phase-3/3.1/3.2，双向闭环）
- **目标（G1→G2→G3）**：`jianpuToStaff(JianpuDoc→Score)` + `Score→MusicXML` 序列化 + round-trip 音高 100% 守恒。
- **任务**：
  - **S1** 复用 L0：直接以已验证的 `JianpuDoc` 为输入。
  - **S2** `jianpuToStaff`：数字/八度点/升降号 → `Note.pitch`（走 MIDI，与 `midiToJianpu` 逆运算）。
  - **S3** `Score→MusicXML`：序列化回 `.musicxml`（pugixml）。
  - **S4** G3 round-trip：五线→简谱→五线，音高 100% 守恒（复用阶段2 单测 + 阶段3 新增 9 项往返测试，当前 117 个 gtest 用例全绿）。
  - **S5** 随 G3 收口补齐阶段2 边界项：小调 6=X、和弦逐音八度点（确保与 L1/L2 记谱一致）；极端连音比 46 处随 G3 同期处理（已单列不计分母）。
  - **S6** 可选 G4：简谱文本输入解析器。
  - **S7** 打 `phase-3` 标签。
- **产出**：反向转换 CLI、往返自测、阶段2 边界项补全、`phase-3`。
- **衔接**：与阶段1 互不依赖；与阶段4（AI）共用 L0/L2/L3；与阶段5（GUI）共用全部模型。

### 阶段 4 — AI / 深度学习 ⬜ 未开始
- **目标**：在 L0/L2 契约上叠加智能能力（如智能分句、错误检测、风格迁移）。
- **任务**：定义训练/推理接口；以阶段3 的往返数据为金标准。
- **产出**：AI 模块 + 评估脚本。
- **衔接**：依赖 M1（阶段3）、M2（阶段1）提供干净数据。

### 阶段 5 — 工程化与 GUI ⬜ 未开始
- **目标**：可分发产品形态（桌面/Web GUI、安装包、CI）。
- **任务**：GUI 封装 CLI；CI（自动 ctest + 覆盖率）；打包。
- **产出**：可安装包、CI 流水线。
- **衔接**：依赖 M1/M2 功能闭环。

### 学习路径总工期与依赖
- 总工期约 **16–23 周**（阶段0 已完成，阶段2 已完成）。
- 依赖 DAG：`L0→L2→L3`、`L0→L1`、`L1→L4`、`L2/L3→L4`、`L2/L3→L5`。
- **推荐顺序**：阶段3（已实现，先学）→ 阶段1（已实现，可并行/后置）→ 阶段4（M3）→ 阶段5（M4）。

### 给学习者的具体练习建议（按已验证契约）
1. **读懂 117 个 gtest 用例（+ 41 个 F3 Python 单测）**：它们是"契约说明书"。改任何纯函数前，先跑对应模块的测试。
2. **红绿练习**：选一个尚未实现的阶段3 函数（如 `jianpuToStaff`），先写一个失败的测试，再实现到绿——这是把"已验证地基"用起来的最佳方式。
3. **交叉验证**：阶段3 每实现一个子功能，立即跑 `ctest --test-dir build --output-on-failure` 确认 117 个 gtest 用例 + 41 个 F3 Python 单测不破，再加往返测试。
4. **文档即进度**：`git push origin main` 已完成（当前 `origin/main == 0736c92`）；阶段3 反向转换已落地。

---

## 附：立即行动清单
1. （历史动作）`git push origin main` 已完成（当前 `origin/main == 0736c92`）。
2. `ctest --test-dir build --output-on-failure` 确认 117 个 gtest 用例 + 41 个 F3 Python 单测全绿。
3. 启动阶段3 G1 `jianpuToStaff`（详见 `stage3_action_plan.md` 的 S1–S10）。
